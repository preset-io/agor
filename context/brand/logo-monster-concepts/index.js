// Sandpack's vanilla template always evaluates /index.js as the app entry.
// The concept board itself is intentionally kept in index.html so the file can
// also be opened directly from the repo without a build step. This tiny entry
// overrides Sandpack's default Hello World script, which otherwise expects a
// #app element and throws before the inline board script can be inspected.
