{
  "name": "@agor/demo-videos",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright + ffmpeg pipeline that renders the /demo/marketing-video fixture into hero demo videos. Standalone install (not a pnpm workspace member) so Playwright stays out of the workspace lockfile.",
  "type": "module",
  "scripts": {
    "capture": "node capture.mjs",
    "encode": "./encode.sh",
    "all": "node capture.mjs && ./encode.sh"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
