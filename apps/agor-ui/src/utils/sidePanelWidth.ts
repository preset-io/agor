/**
 * Width (px) a side panel opens at before the user has resized it — the home
 * teammate sidebar and the board's session panel. Shared so the two cannot
 * drift; each stays independently resizable and keeps whatever width its user
 * has already stored.
 */
export const SIDE_PANEL_DEFAULT_WIDTH_PX = 340;
