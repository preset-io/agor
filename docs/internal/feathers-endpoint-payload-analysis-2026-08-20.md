# Feathers endpoint and payload latency analysis (2026-08-20)

## Status and scope

This analysis starts from one Datadog screenshot from an early Agor Cloud deployment. The panel is a **mean** of `feathers.request.duration_ms` grouped by service, method, and transport. It contains no time window, request counts, percentiles, deployment markers, response sizes, or error split. The values are production observations, not controlled benchmarks.

The current repository was traced and tested on 2026-08-20. It may contain hydration and query improvements that were not in the early deployment. The findings below therefore identify mechanisms and candidates; they do not claim that any one mechanism caused the screenshot.

Per the investigation scope, branch/board authorization predicate construction and RBAC subquery optimization are excluded. Authorization remains an external latency variable. Tenant transaction boundaries are included because they determine PostgreSQL pool occupancy independently of the shape of an RBAC predicate.

## Executive summary

1. The metric measures the outermost Feathers service/hook promise. It includes service hooks, PostgreSQL pool wait and transaction setup/commit, DB work and driver JSON decoding, service enrichment, and outbound/process work awaited by the method. It does **not** measure the subsequent Socket.IO/REST transport write, Socket.IO acknowledgement/client RTT, or MCP response-envelope serialization.
2. The screenshot cannot support causal or capacity conclusions. A mean can be dominated by one cold process invocation, provider outlier, or a handful of startup requests. Counts and p50/p95/p99/max are required.
3. The generic Drizzle adapter often executes `SELECT *` for all candidate rows, parses JSON, then filters, sorts, paginates, and applies `$select` in JavaScript. The nominal generic default and maximum page size are both 10,000, and the client `findAll()` helper auto-pages to the advertised total. `$select` is usually only a wire projection, not a SQL projection.
4. `kb/documents.find` is the clearest unbounded path: the service currently ignores `$limit`, `$skip`, `$sort`, and `$select`. A UI or MCP caller asking for 6 or 10 rows still receives every readable document; `include_content` can hydrate every current version. Its per-document authorization work is excluded from optimization here but remains part of the observed duration.
5. Initial UI hydration starts six background collection reads immediately before a five-request essential batch. On PostgreSQL, each tenant-owned external call normally holds one pool connection/transaction for the full Feathers call. The default pool maximum is 10, so at least 11 near-simultaneous reads can queue even before route-specific work is considered. This is a plausible common explanation for small configuration tables clustering around 2–3 seconds. It is not proven without a production request timeline and pool-wait metric.
6. The isolated measured fix in this change is artifact list projection. The UI already requested only metadata, but the database still returned and decoded `files`. On a PostgreSQL fixture with 500 artifacts and 200 KB of source per artifact, the service metadata list improved from **381.2 ms / 100,335,437 internal JSON bytes** to **13.8 ms / 58,437 result bytes**. Full artifact reads remain unchanged. The MCP list now requests the legacy non-file DTO explicitly.
7. A 50,000-row sessions fixture exposed a non-RBAC index candidate. The board/recent page used a sequential scan plus top-N sort (20.814 ms, 2,632 shared buffers). A temporary `(board_id, archived, updated_at DESC, session_id ASC)` index changed that to an index scan (0.083 ms, 6 shared buffers). The exact count still cost 12.5 ms. No migration is included: a production index should be tenant-aware, validated against real plans, and built concurrently or in a maintenance window rather than through a blocking transactional migration.

## What `feathers.request.duration_ms` measures

The app-level around hook in `apps/agor-daemon/src/metrics/feathers.ts` records `performance.now()` immediately before `await next()` and emits the distribution in `finally`. An `AsyncLocalStorage` marker suppresses nested service-to-service calls, so the chart normally represents one outermost external Feathers call rather than double-counting its fan-out.

### Included

- app and service around/before/after/error hooks;
- authentication/password-change checks executed inside the Feathers call;
- the tenant PostgreSQL transaction, including pool acquisition, `BEGIN`, transaction-local `set_config('agor.tenant_id', ...)`, commit, and registered post-commit callbacks;
- repository SQL, exact count queries, row transfer, and driver-side JSON/JSONB decoding;
- JavaScript filtering, sorting, pagination, `$select`, parsing, decryption, and enrichment;
- awaited nested service calls (their time remains inside the outer call even though they do not emit a second metric);
- awaited provider HTTP, OAuth discovery, model/runtime discovery, child-process/executor work, or filesystem work performed inside the method.

### Not included, or not reliably included

- Socket.IO encoding, transport write, acknowledgement callback, and browser/network round trip after the service promise resolves;
- REST response serialization/write after the service promise resolves (the separate HTTP metric records through response `finish`);
- MCP tool schema parsing/ID resolution that occurs before the instrumented service invocation and the final MCP text/envelope rendering that occurs after it;
- actual asynchronous realtime delivery to subscribed Socket.IO channels. `find` emits no mutation event. Mutation calls can include synchronous publication setup/hook work, but delivery is not an awaited end-to-end acknowledgement.

Consequently, a large JSONB column can increase this metric through DB transfer and decoding even when a late `$select` keeps it off the wire. Conversely, expensive transport serialization after service completion will not be visible here.

### Existing tags and interpretation gaps

Existing tags are bounded: `service`, normalized `method`, `transport`, `outcome`, and `status_code`. Two normalizations lose important identity:

- nonstandard methods all become `method:custom`, so the 504.1 ms `users/custom/socketio` row combines unrelated user operations;
- service paths containing characters outside `[a-zA-Z0-9_/-]` become `service:other`. Registered parameterized paths containing `:` can therefore combine in the `other/create` rows.

Socket.IO and MCP rows invoke the same underlying service/hook code, but callers frequently use different query shapes. For example, current UI session hydration asks for `updated_at` order, which can use the SQL page path, while some MCP lists use a shape that falls back to whole-table materialization. Their difference is not evidence of Socket.IO overhead.

### Recommended Datadog views and bounded instrumentation

Always pair the duration distribution with `feathers.requests` count and show p50, p95, p99, and max, separated by success/error. Add deployment/environment/region as normal global tags. Overlay process restart/deploy markers.

Safe bounded dimensions:

- an allow-listed `operation` for custom and parameterized registered services;
- `query_shape`: `global`, `board`, `session`, `recent`, `keyset`, `id-set`, or `other`;
- `projection_shape`: `full`, `metadata`, `without-blob`, `id-only`, or `other`;
- `limit_bucket`: `none`, `1-25`, `26-100`, `101-1000`, `1001-10000`;
- `result_shape`: `single`, `array`, `page`;
- `process_age`: `cold-0-60s`, `warm`.

Do not tag tenant, user, board, branch, session, task, artifact, MCP server, hostname, URL, or raw query values.

Prefer separate distributions/gauges, with only bounded phase/provider-class tags, for:

- DB pool acquisition duration and active/idle/pending pool counts;
- DB transaction duration, SQL duration, and query count per outer request;
- row/result bytes before service projection;
- response serialization duration and response bytes at Socket.IO/REST/MCP boundaries;
- provider/process duration (`openai-embedding`, `mcp-oauth`, `opencode-runtime`, etc.);
- service phases such as `authorize`, `count`, `page`, `hydrate`, `decrypt`, `provider`.

## Common query and hydration behavior

### Generic adapter

For most services, `DrizzleService.fetchData()` calls the repository's `findAll()`. The common behavior is:

1. repository `SELECT *` (sometimes with a small set of pushed predicates);
2. row/JSON conversion;
3. JavaScript filtering and sort;
4. total from the in-memory filtered array;
5. JavaScript skip/limit;
6. JavaScript `$select`.

Generic pagination is `default=10,000`, `max=10,000`. The shared client `findAll()` follows paginated continuation pages until it reaches `total`. A small `$limit` therefore does not necessarily bound database work, and `findAll({$limit: 25})` means “retrieve the entire stable membership in 25-row pages,” not “retrieve only 25.” Tasks/messages have specialized keyset loops, and sessions have a specialized SQL page path for a limited set of query shapes.

### Initial UI request topology

Current `useAgorData` attaches realtime handlers, then starts these non-gated reads:

1. agentic-tool-settings;
2. mcp-servers;
3. session-mcp-servers;
4. gateway-channels;
5. artifacts metadata;
6. mcp-servers/oauth-status.

It then starts an awaited light batch:

7. recent sessions (single page on first load);
8. lean boards;
9. card-types;
10. repos;
11. users.

After resolving the displayed board it starts the board-scoped heavy batch (branches, sessions, board-objects, board-comments, cards, and one full board), then background global hydration for collections that require complete realtime state. Knowledge routes and settings components can add requests. Some global hydration is intentional for realtime correctness; replacing it with a silent hard cap would make the store incomplete.

The default postgres.js pool maximum is 10. Tenant-owned external services usually hold a transaction for the whole call. Therefore the current launch order can queue an essential read behind background work. Reordering/throttling requires a browser test that preserves the quiet-window/generation protections and proves first-paint and eventual global completeness.

## Endpoint-by-endpoint trace

“Late select” means the DB already returned/decoded the omitted fields. “Exact count” means an additional `COUNT(*)` query. Index notes exclude RBAC predicates.

| Observation                                          | Current call/query and payload behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Assessment / next evidence                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `kb/documents find socketio` 13,510.7 ms             | UI Home/Knowledge reads call `find`; MCP teammate memory also calls it. The service returns an array and ignores `$limit`, `$skip`, `$sort`, `$select`, and plain `limit`. It selects all matching document metadata, separately reads all namespaces to construct URLs, then performs readability checks and attribution/indexing enrichment. `include_content` batch-loads current versions; a requested historical version can become per-document lookup. Metadata can contain JSON; content is an unbounded text/blob risk. Document namespace/path, kind, visibility, status, updated, and archived indexes exist. | Highest-confidence correctness/payload problem. Honor explicit limits without breaking full-inventory callers; ideally apply a visibility-aware SQL page before hydration. Per-document authorization cost is intentionally not optimized here. Controlled 5,000-row result: 8.39 MB/74.6 ms for all metadata vs 16.7 KB/6.0 ms for SQL limit 10, excluding authorization/content. |
| `boards find socketio` 5,067.8 ms                    | Initial UI requests `lean:true`, but current repository selects/decodes the full `data` JSONB and the service removes `objects` and `custom_css` afterward. `$select` is also late. The UI later gets the displayed full board and background-hydrates all full boards for switching/realtime correctness. No SQL count; generic in-memory total/page. Default/max 10,000. Indexes: tenant, name, slug; no archive index.                                                                                                                                                                                                | Push the lean JSONB projection into SQL, then retain lazy full-board hydration. One 400 KB synthetic board: 401,397 bytes/4.7 ms full vs 1,359 bytes/1.6 ms using SQL JSONB subtraction. Production bytes and object-size distribution are needed before changing the hydration contract.                                                                                          |
| `sessions find socketio` 4,056.6 ms                  | UI first paint asks for one recent page; displayed-board and background hydration use `findAll`. A supported query shape uses `SessionRepository.findPage`: full session rows plus an exact `COUNT(*)`, then relationship enrichment. Other shapes fall back to `SELECT *` for all sessions and generic JS processing. Session `data` contains context, task/config state, and historical metadata. `$select` is late outside specialized shapes. Existing indexes cover tenant/status/board/branch/created, not `updated_at` ordering.                                                                                  | Add phase/query-count/pool metrics first. Validate tenant-aware recent/board composite indexes. Consider count-free `limit+1` first paint and keyset global hydration, but preserve `findAll` stable membership. Controlled plan improved 20.814 ms/2,632 buffers to 0.083 ms/6 buffers with a temporary board+archived+updated+id index; count remained 12.5 ms.                  |
| `repos find socketio` 2,986.7 ms                     | Initial global UI load. Repository selects every full row; generic JS pagination/select. Repo `data` can contain both v2 environment variants/template overrides and a legacy environment view. No count query. Default/max 10,000. Indexes: tenant and slug. No external provider work.                                                                                                                                                                                                                                                                                                                                 | Usually low cardinality, so ~3 s is more consistent with pool queue/outlier than a repo scan unless environment JSON is unusually large. Measure row/result bytes; add a list projection if large.                                                                                                                                                                                 |
| `card-types find socketio` 2,961.9 ms                | Initial global UI load. Full tiny table, no SQL count, generic JS paging/select. `json_schema` is stored as text and parsed for every selected row before any late projection. Indexes: tenant/name. No enrichment or network work.                                                                                                                                                                                                                                                                                                                                                                                      | Strong control group for shared pool wait, cold start, or low-N mean. Only optimize row parsing if schemas are demonstrably large.                                                                                                                                                                                                                                                 |
| `artifacts find socketio` 2,616.1 ms                 | Initial UI already supplies a metadata `$select`; before this change the repository still selected/decoded `files`, dependencies, Sandpack config, env/grant JSON, then projected. Generic in-memory page/total; board/archived/branch predicates are pushed. Indexes exist separately for tenant, board, branch, source session, archived, public.                                                                                                                                                                                                                                                                      | Fixed in this change for explicit non-file selects. Metadata selects use SQL metadata columns; non-file legacy selects retain all other fields. Full/get/payload paths remain full. Controlled service path: 381.2 ms/100.34 MB to 13.8 ms/58.4 KB.                                                                                                                                |
| `session-mcp-servers find socketio` 2,495.3 ms       | Initial global UI load. The service directly reads relationship rows and performs JS visibility filtering. It returns a raw array and does not enforce transport pagination despite UI passing `$limit`. Rows are small; indexes cover tenant, session, server, and `(session,enabled)`. No provider work.                                                                                                                                                                                                                                                                                                               | Add true bounded/global snapshot semantics only with realtime completeness design. Small rows make pool queue or external authorization more likely than payload.                                                                                                                                                                                                                  |
| `mcp-servers find socketio` 2,397.7 ms               | Initial global UI load. Filter predicates are partly pushed, but full server `data` is read: encrypted environment/headers/auth plus discovered tools/resources/prompts and permissions. Pagination/sort are in JS. An after hook injects per-user OAuth state with one token lookup per OAuth server (parallel) and then redacts secrets. Indexes cover tenant/name/scope/owner/enabled.                                                                                                                                                                                                                                | Add safe list projection and batch OAuth-token lookup. Measure discovered-capability JSON bytes and query count. No provider network call occurs on normal find. Pool queue remains plausible.                                                                                                                                                                                     |
| `opencode-models find socketio` 2,297.2 ms           | UI model selectors use a shared client-side single-flight cache. This is an identity-only service: it opens only short DB units, then invokes the packaged OpenCode runtime/model-catalog process and reads runtime/auth state. It is not a configuration-table scan and does not hold a request-wide DB transaction.                                                                                                                                                                                                                                                                                                    | The duration can legitimately be process startup/filesystem/runtime discovery, especially cold. Add process/provider phase duration and cold/warm cache metrics before introducing server caching/invalidation.                                                                                                                                                                    |
| `agentic-tool-settings find socketio` 2,278.6 ms     | Initial global UI load. `find` runs `publicSettings` for each supported agentic tool in parallel; each independently reads/decrypts/parses its `app_variables` namespace. Result is tiny. App-variable indexes include tenant and unique tenant+namespace+key.                                                                                                                                                                                                                                                                                                                                                           | Batch the namespace/key reads only after query-count measurement. Pool queue plus several small reads is more plausible than a table scan.                                                                                                                                                                                                                                         |
| `gateway-channels find socketio` 2,206.3 ms          | Initial global UI load. Repository selects all full channel rows, including encrypted provider `config`, agentic config, MCP IDs, and listener JSON, then decrypts sensitive fields/env values for each row; after hooks redact them again for transport. No provider call on find. Generic 10,000 page. Indexes cover tenant, channel key, enabled/type, presets, and listener discovery/lease.                                                                                                                                                                                                                         | Add an explicit safe list projection that never retrieves/decrypts secret config unless the settings detail view needs it. Measure decrypt count/bytes. On a small installation, pool queue remains the leading shared explanation.                                                                                                                                                |
| `mcp-servers/oauth-status find socketio` 2,190.7 ms  | Initial global UI load. Reads caller token rows, then resolves servers individually, producing an avoidable server N+1; evaluates expiry/decrypt state. No provider network lookup on normal status.                                                                                                                                                                                                                                                                                                                                                                                                                     | Batch server lookup and expose query count. Pool queue may dominate at small N.                                                                                                                                                                                                                                                                                                    |
| `users find socketio` 2,114.1 ms                     | Initial global UI load and some dialogs. Service/repository select every internal user row, including password hash and large encrypted credentials/env/default config JSON, then search/sort/page in JS and sanitize/map the requested page. `$limit` does not constrain SQL; `$select` is not a safe DB projection. Indexes: tenant/email.                                                                                                                                                                                                                                                                             | High-value safe-list SQL projection: public list fields only, with explicit detail path for credential-bearing settings. Add server-side paging/search. Pool queue likely explains seconds at low user counts; encrypted blob size can amplify it.                                                                                                                                 |
| `sessions find mcp` 1,616.2 ms                       | MCP session tools call the same service but use different sorts/filters and compact results only after service return. Some shapes miss the SQL page fast path and materialize full session JSON before MCP compaction. MCP envelope rendering is outside this metric.                                                                                                                                                                                                                                                                                                                                                   | Normalize MCP list queries to the SQL page/keyset shape and pass explicit list projections. Compare identical query shapes across transports before attributing any difference to transport.                                                                                                                                                                                       |
| `mcp-servers get socketio` 1,551.4 ms                | PK/short-ID server lookup reads full server data, then caller-specific OAuth token lookup/decrypt/redaction. Usually initiated by settings/detail flows. No provider probe. Primary-key lookup is indexed.                                                                                                                                                                                                                                                                                                                                                                                                               | Likely token/decrypt/pool/outlier rather than scan. Add phase timing; avoid full discovered-capability blobs when the caller needs only metadata.                                                                                                                                                                                                                                  |
| `mcp-servers/oauth-start create socketio` 1,190.1 ms | Identity-only endpoint. Uses short durable DB units but performs OAuth protected-resource/authorization-server metadata discovery, dynamic registration/PKCE setup, and other provider network work.                                                                                                                                                                                                                                                                                                                                                                                                                     | Provider latency is expected and must be separated from DB/pool metrics. Tag only bounded provider phase/outcome, never provider URL or server ID.                                                                                                                                                                                                                                 |
| `other create socketio` 745.8 ms                     | Multiple registered paths can collapse to `service:other`, especially parameterized service paths containing `:`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Not attributable. Fix bounded operation normalization before investigating.                                                                                                                                                                                                                                                                                                        |
| `boards get socketio` 728.7 ms                       | UI deliberately fetches the displayed board's full JSON (objects/custom CSS) for first paint. PK lookup plus row JSON decode; no count/network.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Record response/internal row bytes. Large annotations are a credible direct cause, while pool wait remains possible. Keep lazy full hydration for correctness.                                                                                                                                                                                                                     |
| `users custom socketio` 504.1 ms                     | All nonstandard user methods share one metric label; operations include credential/config/avatar/git environment flows with very different behavior.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Not attributable without allow-listed operation identity.                                                                                                                                                                                                                                                                                                                          |
| `other create mcp` 469.3 ms                          | Same lossy service normalization as the Socket.IO row; MCP wrapper time outside the service is not measured.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Not attributable without operation identity.                                                                                                                                                                                                                                                                                                                                       |
| `board-objects find socketio` 439.3 ms               | UI board-scoped first, then global background. Service executes page query and exact count concurrently. Board/branch/card filters are SQL-pushed. If `$limit` is omitted, repository pagination options are empty (all rows) even though response reports limit 100; UI currently supplies 10,000. `position/zone_id` live in JSON. Indexes: tenant/board/branch/card; no expression index for JSON `zone_id`.                                                                                                                                                                                                          | Enforce coherent explicit limit semantics without truncating global realtime snapshot callers. Count-free/keyset global hydration is preferable. Zone expression index matters only if zone-filter plans prove it.                                                                                                                                                                 |
| `kb/search find mcp` 410.2 ms                        | Text mode is capped at 101 but uses leading-wildcard `lower(title/path/content) LIKE` predicates over current version content, can fetch at least 100 candidates, then scores/sorts/slices in JS and attaches attribution/indexing. Semantic/hybrid mode also calls the OpenAI embedding provider and pgvector; hybrid currently runs text then semantic sequentially.                                                                                                                                                                                                                                                   | Split text/semantic/hybrid metrics. For text, validate FTS/trigram GIN rather than a B-tree; for semantic, measure embedding provider separately. The 410 ms aggregate cannot distinguish modes.                                                                                                                                                                                   |
| `board-comments find socketio` 364.2 ms              | UI board-scoped first, then global. SQL page plus exact count; full Markdown content, reactions JSON, and position/mentions JSON. Default/max 10,000. Indexes cover board/session/task/message/branch/creator/parent/created/resolved.                                                                                                                                                                                                                                                                                                                                                                                   | Global completeness is intentional today. Consider keyset/global background hydration and count-free first page; payload projection is less useful because comments need content.                                                                                                                                                                                                  |
| `branches find socketio` 305.0 ms                    | Board/repo/archived/id filters are SQL-pushed, but all matching full branch rows are transferred before generic JS page/sort/select. Batch zone enrichment follows. Branch JSON can contain environment/custom-context/runtime state. Default/max 10,000. Indexes include tenant/repo/board/name/ref/created/updated; no standalone archived index.                                                                                                                                                                                                                                                                      | Current board scoping is already the main non-RBAC win. Measure branch JSON bytes. A global archived filter may scan, but an archived index is low-selectivity and should be plan-driven.                                                                                                                                                                                          |
| `cards find socketio` 275.9 ms                       | Board/archived predicates are pushed; search and generic paging/select remain in JS after all matching full rows. Payload can include note/description/custom data JSON. Default/max 10,000. Indexes: tenant/board/card type/title/archived/created.                                                                                                                                                                                                                                                                                                                                                                     | Consider combined board+archived ordering only if plans justify it. Use bounded/keyset background hydration rather than silently truncating cards needed by the board/store.                                                                                                                                                                                                       |
| `session-streams create socketio` 210.9 ms           | Capability-only initial announce is in-memory and reads no session. A real subscription calls `sessions.get` to validate/resolve access, then joins the connection to an in-memory channel. Both shapes share one metric label. No event delivery/ack is included.                                                                                                                                                                                                                                                                                                                                                       | Add operation shape (`capability` vs `subscribe`) and nested phase timing. The aggregate mean is not actionable.                                                                                                                                                                                                                                                                   |
| `tasks find socketio` 209.8 ms                       | Specialized SQL page with validated filters/sorts, exact `COUNT(*)`, and bounded limit. Session hydration uses a verified task-ID keyset loop. Only the exact `$select:['task_id']` shape projects in SQL; other `$select`s read full task JSON (prompt, raw/normalized SDK response, report, metadata) before late projection. Indexes cover session, `(session,task_id)`, status, created, `(session,created)`, queue/runtime paths.                                                                                                                                                                                   | Measure task JSON bytes and count time. Extend safe SQL projections for list summaries; consider count-free pages where callers do not need total.                                                                                                                                                                                                                                 |
| `cards find mcp` 188.7 ms                            | Same cards service. MCP supplies bounded page inputs, but repository work is still all matching board rows before generic pagination; MCP result rendering is outside metric.                                                                                                                                                                                                                                                                                                                                                                                                                                            | Align to a SQL page/projection and compare identical Socket.IO/MCP query shapes.                                                                                                                                                                                                                                                                                                   |
| `branches patch socketio` 154.6 ms                   | Reads current row, performs validation, row lock/re-read, deep JSON merge/update, zone enrichment, and relevant hooks. A board move can remove/create board-object placement. Normal patch does not perform provider discovery. PK and board-object references are indexed.                                                                                                                                                                                                                                                                                                                                              | Reasonable multi-query mutation path. Add query count/phase metrics before changing atomicity or realtime behavior.                                                                                                                                                                                                                                                                |
| `sessions create mcp` 152.8 ms                       | Validates/resolves branch/repo/user/config context, inserts a full session row, establishes defaults/relationships, and runs hooks. MCP ID/tool preparation outside the service is not included.                                                                                                                                                                                                                                                                                                                                                                                                                         | Already below the read outliers. Preserve atomic/session lifecycle semantics; inspect only if p95 and query count regress.                                                                                                                                                                                                                                                         |
| `config/resolve-api-key create socketio` 149.4 ms    | Executor-only flow validates token scope, calls `tasks.get`, may call `sessions.get` to verify tool scope, then reads/decrypts per-user/tenant credential data in a short scoped DB unit. No external provider call. Response contains one secret and is intentionally not cacheable.                                                                                                                                                                                                                                                                                                                                    | Query/decrypt count is expected. Keep security boundaries; optimize only with scoped batch reads and never add key identity/value tags.                                                                                                                                                                                                                                            |
| `kb/document-edits create mcp` 136.4 ms              | Loads the full current document/version, applies ops and constructs a full diff in memory. A real write creates a version, rechunks Markdown, replaces search units, checks semantic settings/pgvector, resolves graph links (potentially sequential by link), syncs edges, and wakes the async indexer. Provider embedding is not awaited here. Response includes diff and optionally full content.                                                                                                                                                                                                                     | Add document-byte, op-count bucket, link-count bucket, and phases. Batch link resolution and cap response/diff bytes only with explicit API semantics.                                                                                                                                                                                                                             |
| `tasks get mcp` 121.2 ms                             | Indexed PK/short-ID lookup but returns the entire task JSON, including potentially large raw SDK response/report/prompt. MCP tool may compact only afterward.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Add a summary projection for MCP callers that do not request full execution detail; record internal/result bytes.                                                                                                                                                                                                                                                                  |
| `sessions patch socketio` 117.8 ms                   | Indexed read/lock/deep-merge/write of session JSON plus lifecycle/realtime hooks and any relationship maintenance. No normal provider lookup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | No clear isolated issue at this mean. Preserve lifecycle correctness.                                                                                                                                                                                                                                                                                                              |

## Controlled PostgreSQL reproduction

These tests ran against local PostgreSQL 16 with generated data and warm-process wall timing. They demonstrate scaling mechanisms, not cloud latency. Authorization predicate cost was excluded.

| Shape                                                                                           | Result                                                                                 |
| ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| 500 artifacts, 200,000 source bytes each, direct full query                                     | 419.0 ms; 100,426,785 JSON bytes                                                       |
| Same artifacts, metadata SQL projection                                                         | 8.0 ms; 293,785 JSON bytes (99.71% smaller)                                            |
| Actual artifact service full list after change                                                  | 381.2 ms; 100,335,437 result bytes                                                     |
| Actual artifact service metadata `$select` after change                                         | 13.8 ms; 58,437 result bytes                                                           |
| 5,000 KB metadata rows with ~1 KB JSON metadata                                                 | 74.6 ms; 8,394,680 bytes                                                               |
| Same KB query, SQL limit 10                                                                     | 6.0 ms; 16,710 bytes                                                                   |
| One board with 200 KB objects + 200 KB CSS                                                      | 4.7 ms; 401,397 bytes                                                                  |
| Same board, SQL JSONB subtraction of heavy keys                                                 | 1.6 ms; 1,359 bytes                                                                    |
| 50,000 sessions, board+archived recent page, before candidate index                             | 19.4 ms wall; EXPLAIN execution 20.814 ms; sequential scan/top-N; 2,632 shared buffers |
| Exact count for that session filter                                                             | 12.5 ms                                                                                |
| Same page with temporary board+archived+updated+id index                                        | 2.6 ms wall; EXPLAIN execution 0.083 ms; index scan; 6 shared buffers                  |
| Pool max 10; ten actively executing `pg_sleep(0.5)` calls; six `SELECT 1` submitted after 80 ms | every nominally trivial query waited 420.6–420.7 ms                                    |

The pool test establishes that a slow shared dependency can make unrelated tiny endpoints report nearly the same Feathers duration. It does not prove that the screenshot captured this condition. Production confirmation requires pending-pool telemetry correlated with a per-request timeline.

## Implemented change

Only the artifact list projection was implemented because it is isolated, preserves default/full behavior, and has a large controlled improvement.

- Added the canonical `ARTIFACT_LIST_FIELDS_WITHOUT_FILES` transport field list.
- Added repository projections:
  - `full` (unchanged default);
  - `without-files` (legacy list DTO without source files);
  - `metadata` (omits source/runtime JSON fields).
- `ArtifactsService.fetchData` chooses a SQL projection only when the caller explicitly supplies a `$select` that excludes `files`.
- Existing callers with no `$select`, `get`, payload/render, publish, export, and mutation paths still retrieve full rows.
- `agor_artifacts_list` now explicitly requests the non-file DTO, so MCP no longer retrieves source blobs merely to strip them after service return.
- Added repository, service, and MCP regression coverage.

This is tenant-safe: the existing trusted tenant transaction/RLS context and board/branch filters are unchanged. Projection only removes columns. No RBAC query construction changed.

## Prioritized follow-up

| Priority | Candidate                                                                                                                               | Expected impact                                   | Confidence                                              | Effort / risk                            |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------- | ---------------------------------------- |
| P0       | Add counts/percentiles, bounded operation/query/projection tags, DB pool wait/query count, provider phases, and internal/response bytes | Makes every subsequent conclusion defensible      | High                                                    | Medium; instrumentation only             |
| P0       | Production trace of one cold and warm UI load with Socket.IO request timeline plus pool active/idle/pending                             | Confirms/refutes shared startup contention        | High                                                    | Low-medium                               |
| P1       | Honor explicit KB document limits and add a visibility-aware SQL page before content hydration                                          | Very high for large KBs/MCP memory payloads       | High for payload; latency depends on excluded auth work | Medium-high; caller audit required       |
| P1       | Safe SQL projections for users, MCP server lists, gateway channel lists, and lean boards                                                | High when encrypted/discovered/blob JSON is large | High                                                    | Medium; detail/realtime contract tests   |
| P1       | Reorder or concurrency-limit non-gated hydration so essential reads acquire pool slots first                                            | Potentially high first-paint improvement          | Medium until production pool telemetry                  | Medium-high; realtime quiet-window tests |
| P1       | Validate and concurrently roll out session recent-order indexes; remove exact counts from callers that only need `hasMore`              | High at large session cardinality                 | High from synthetic plan, needs real RLS/tenant plan    | Medium; online-index/runbook work        |
| P2       | Batch MCP OAuth-status server lookup and OAuth token injection; batch agentic setting reads                                             | Reduces small-query fan-out                       | High                                                    | Low-medium                               |
| P2       | SQL page/projection support in the generic adapter, service by service rather than a global semantic flip                               | Broad scaling improvement                         | High                                                    | High compatibility risk                  |
| P2       | PostgreSQL text-search index (FTS or pg_trgm) after representative KB search plans                                                      | High for content search                           | Medium-high                                             | Medium-high; extension/index rollout     |
| P3       | Keyset background hydration for sessions/branches/cards/comments/objects while maintaining stable membership and realtime overlay       | Avoids deep offsets/exact counts and huge pages   | Medium                                                  | High                                     |

### Index rollout cautions

- Do not add a normal blocking index migration to a large production sessions table without measuring lock duration and write load.
- PostgreSQL `CREATE INDEX CONCURRENTLY` cannot run inside the normal transactional migration path. Use a reviewed operational migration/runbook or a maintenance window, then add the schema declaration in a coordinated release.
- Test both global recent and board-scoped recent plans under the production RLS/tenant policy. Likely candidates are tenant-aware variants of `(tenant_id, archived, updated_at DESC, session_id ASC)` and `(tenant_id, board_id, archived, updated_at DESC, session_id ASC)`; do not ship both without write/storage measurements.
- Exact `COUNT(*)` is not fixed by an ordering index. Where total is not displayed, prefer `limit+1`/`hasMore`; where complete snapshots are required, use keyset iteration.

## Validation performed

- focused core artifact repository tests;
- focused daemon artifact service and MCP tool tests;
- a PostgreSQL-specific artifact service lane using normal migrations and a 100 MB fixture;
- controlled PostgreSQL `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)` for session page before/after a temporary index;
- controlled PostgreSQL pool-saturation test;
- focused Biome checks on changed files;
- existing UI hydration tests are part of final validation because no UI behavior was intentionally changed.

Workspace-wide type checks require built workspace package artifacts in this checkout and fail at unresolved `@agor/core/*` / `@agor/git` imports before reaching the changed code. Per repository instructions, no workspace build was run solely to manufacture those artifacts. The focused TypeScript/Vitest transforms and lint checks cover the modified paths.

## Residual unknowns

- Datadog time window, sample count per group, p50/p95/p99/max, success/error mix, process age, deploy/restart timing, tenant/data distribution, and DB instance utilization;
- early deployment commit relative to the current query/hydration code;
- actual production response and pre-projection row bytes;
- pool active/idle/pending counts and pool wait time during initial UI load;
- DB vs hook vs authorization vs provider decomposition;
- which concrete operations are represented by `other/create` and `users/custom`;
- which KB search modes are represented in the aggregate;
- production PostgreSQL plans under real tenant RLS policies, statistics, cache state, and table bloat;
- provider/network latency distributions for OAuth and model discovery.

Until those are measured, the correct reading of the screenshot is: **there are several concrete unbounded/blob-fetch mechanisms worth fixing, and there is a plausible shared startup/pool mechanism, but the chart alone cannot assign causality or rank database capacity changes.**
